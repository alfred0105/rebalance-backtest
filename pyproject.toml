[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rebalance-backtest"
version = "0.6.0"
description = "Backtest and compare daily-monitored Korean ETF rotation variants with sparse band rebalancing and crash braking."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.26",
  "pandas>=2.1",
  "matplotlib>=3.8",
  "yfinance>=0.2.54",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
rebalance-backtest = "rebalance_backtest.cli:main"
rebalance-rotation = "rebalance_backtest.rotation_cli:main"
rebalance-sweep = "rebalance_backtest.sweep_cli:main"\nrebalance-all = "rebalance_backtest.runner_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
